node_modules
dist

# Local environment files may contain credentials.
.env
.env.*
!.env.example
!.env.*.example
.envrc
.direnv/

# Private keys and local secret stores.
.secrets/
secrets/
.ssh/
*.key
*.pem
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_ed25519
credentials*.json
service-account*.json
.netrc
.npmrc
.yarnrc
.pnpmrc
!.npmrc.example
!.yarnrc.example
!.pnpmrc.example
certbot/
letsencrypt/
acme/

# Local Compose overrides can contain ports, mounts, and credentials.
docker-compose.override.yml

.agents
.playwright-mcp

.DS_Store
Thumbs.db

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
logs/

coverage

.idea
.vscode

# Paid font files must stay local and must not be committed.
miniapp/src/assets/fonts/
miniapp/docs/

# Local databases and database backups. Do not ignore all *.sql files:
# Prisma migrations and Docker initialization scripts must stay tracked.
.data
data
*.db
*.sqlite
*.sqlite3
backups/
*.dump
*.dump.gz
backup*.sql
backup*.sql.gz
*-backup.sql
*-backup.sql.gz
